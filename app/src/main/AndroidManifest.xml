<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!-- 商米扫码服务权限 -->
    <uses-permission android:name="com.sunmi.scanner.permission.SCANNER" />

    <!-- Android targetSdk >= 30 必须声明，用于 AIDL 绑定扫码服务 -->
    <queries>
        <package android:name="com.sunmi.scanner" />
    </queries>

    <application
        android:name=".MfApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/backup_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.MfPda"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".ui.splash.SplashActivity"
            android:theme="@style/Theme.Splash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.login.LoginActivity"
            android:theme="@style/Theme.Login"
            android:exported="false"
            android:windowSoftInputMode="stateVisible|adjustResize" />

        <activity android:name=".ui.mime.MimeActivity" android:exported="false" />
        <activity android:name=".ui.manager.ManagerActivity" android:exported="false" />
        <activity android:name=".ui.modifyPW.ModifyPwActivity" android:exported="false" />
        <activity android:name=".ui.ordertotal.OrdertotalActivity" android:exported="false" />
        <activity android:name=".ui.goodlist.GoodlistActivity" android:exported="false" />
        <activity android:name=".ui.orderConfirm.OrderConfirmActivity"
            android:exported="false"
            android:windowSoftInputMode="stateVisible|adjustResize" />
        <activity android:name=".ui.receiveList.ReceiveListActivity" android:exported="false" />
        <activity android:name=".ui.receiveDetail.ReceiveDetailActivity" android:exported="false" />
        <activity android:name=".ui.addressManager.AddressManagerActivity" android:exported="false" />
        <activity android:name=".ui.addressAdd.AddressAddActivity" android:exported="false" />
    </application>
</manifest>
